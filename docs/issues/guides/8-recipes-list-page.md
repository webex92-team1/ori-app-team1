# Issue #8: レシピ一覧ページでRakuten APIを表示

作業ブランチ：feature/issue-8-recipes-list-page

## 🎯 このタスクの目的

`/recipes` ページを検索クエリに応じて `searchRecipesByIngredients` を呼び出す実装に置き換え、ローディング／エラー／空状態を制御します。

---

## 📁 作業で触るファイル

- `src/app/recipes/page.jsx`

⚠️ **以下のファイルは触らない**

- `src/app/recipes/[id]/page.jsx`
- `src/lib/mockdata.js`
- `src/app/ingredients/page.jsx`
- `src/lib/recipes.js`

---

## 🌲 作業を始めるとき

作業ブランチに移動する

```bash
git checkout -b feature/issue-8-recipes-list-page
```

---

## 🧩 作業内容（やること）

1. `useSearchParams` で `ingredients` クエリを読む
2. クエリが存在しない場合は「食材を入力してください」の空状態を表示
3. `useEffect` + `AbortController` で API を呼び、取得完了までローディングカードを表示
4. ネットワークエラー時はトーストまたはエラーカードを表示
5. カテゴリフィルターが API レスポンスに対して機能するようにする
6. カードクリックで `/recipes/[id]` に遷移

---

## ✅ 完成条件（チェックリスト）

- [ ] `useSearchParams` で `ingredients` クエリを読み、存在しない場合は空状態を表示
- [ ] `useEffect` で API を呼び、取得完了までローディングカードを表示
- [ ] ネットワークエラー時はエラーカードを表示し、フォールバックデータで画面が崩れない
- [ ] カテゴリフィルターが API レスポンスに対して機能し、選択中は強調スタイルが付く
- [ ] カードクリックで `/recipes/[id]` に遷移する

---

## 🔍 動作確認のしかた

1. `/ingredients` から「卵 玉ねぎ」で検索し `/recipes` に遷移
2. ローディングスケルトン → レシピカードが表示されることを確認
3. 存在しない食材（例: `xyz`）で検索し、0 件メッセージが表示されることを確認
4. 開発者ツールでネットワークを Offline にし再読み込みし、エラーメッセージとフォールバック表示が出ることを確認

---

## 🔗 関連・順番

- このタスクの前に終わっている必要がある: #2, #6, #7
- このタスクが終わったら進められるもの: #9
- ラベル: `frontend`

---

## 🌲 作業が完了したら

1. `git add .`
2. `git commit -m "feat: レシピ一覧ページでRakuten APIを表示"`
3. `git push origin feature/issue-8-recipes-list-page`
4. GitHub上でPRを作成

---

## 💡 ちょっとしたヒント

- `useSearchParams()` で URL パラメータを取得: `searchParams.get('ingredients')`
- `useEffect` でのクリーンアップ: `return () => controller.abort();`
- ローディング状態: `useState(true)` で管理
- カテゴリフィルター: `recipes.filter(r => r.category === selectedCategory)` のようにフィルター

---

## 💬 メモ・質問（必要なら）

- `searchRecipesByIngredients` は `src/lib/recipes.js` で定義されています

